let p=G.sh[i];p.x+=p.v*dt;if(!p.t||p.t.dead){G.sh.splice(i,1);continue}if(Math.abs(p.x-p.t.x)<24){p.t.h-=p.a;if(p.t.h<=0)kill(p.t);G.sh.splice(i,1)}}G.u=G.u.filter(x=>!x.dead||x.death>0);if(!G.q.length&&!G.u.some(x=>x.team==='e'&&!x.dead)){if(G.w<6){G.w++;G.q=[...W[G.w-1]];G.next=2;G.gap=Math.max(1.05,G.gap-.1);G.mana=Math.min(500,G.mana+80)}else fin(1)}if(G.php<=0)fin(0);if(G.ehp<=0)fin(1);hud()}
function extra(){ctx.lineWidth=3;for(const p of G?.sh||[]){ctx.strokeStyle=p.team==='p'?'#e8f3ff':'#ffb0a0';ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x-p.v*.035,p.y);ctx.stroke()}}function fin(w){if(G.over)return;G.over=1;ov.style.display='grid';if(w){let st=G.php/G.mph>.75?3:G.php/G.mph>.4?2:1,r=350+G.stage*45+G.gold;S.gold+=r;S.cr+=G.boss?8:2;S.ore+=G.boss?1:0;S.stars[G.stage]=Math.max(S.stars[G.stage]||0,st);S.unlock=Math.max(S.unlock,Math.min(24,G.stage+1));save();obox.innerHTML=`<h1>VICTORY<i>RAVENHOLD ADVANCES</i></h1><p class=m>${st}★ · ${G.kills} kills · ${r} gold</p><button class="b r" onclick="hub('campaign')">RETURN</button>`}else obox.innerHTML=`<h1>DEFEAT<i>THE LEGION HOLDS</i></h1><button class="b r" onclick="begin(${G.stage})">RETRY</button> <button class=b onclick="hub('campaign')">RETURN</button>`}
function loop(t){let dt=Math.min(.033,(t-last)/1e3);last=t;if(battle.classList.contains('on')&&G){db();upd(dt);[...G.u].sort((a,b)=>a.y-b.y).forEach(x=>du(x,dt));extra()}requestAnimationFrame(loop)}hero.onclick=her;met.onclick=meteor;document.querySelectorAll('[data-or]').forEach(x=>x.onclick=()=>order(x.dataset.or));document.querySelectorAll('[data-sp]').forEach(x=>x.onclick=()=>{if(G)G.sp=+x.dataset.sp;document.querySelectorAll('[data-sp]').forEach(y=>y.classList.toggle('on',y===x))});pa.onclick=()=>{if(G)G.pause=!G.pause};dev.onclick=()=>dp.classList.toggle('on');document.querySelectorAll('[data-ch]').forEach(x=>x.onclick=()=>{if(x.dataset.ch==='gold'){S.gold+=5e3;save()}if(x.dataset.ch==='unlock'){S.unlock=24;save()}if(G&&x.dataset.ch==='mana')G.mana=500;if(G&&x.dataset.ch==='win')G.ehp=0;rr()});document.addEventListener('keydown',e=>{if(e.key==='F2')dp.classList.toggle('on');if(!battle.classList.contains('on'))return;let k=e.key.toLowerCase();if('1234'.includes(k))dep(['knight','spear','archer','cavalry'][+k-1]);if(k==='q')her();if(k==='w')meteor();if(k==='a')order('assault');if(k==='s')order('hold');if(k==='d')order('retreat');if(e.code==='Space')G.pause=!G.pause});assets().then(()=>requestAnimationFrame(loop)).catch(e=>{console.error(e);L.textContent='Asset load failed — refresh once';L.style.color='#f88'});
</script>
