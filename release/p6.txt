na<180||G.spell>0)return;G.mana-=180;G.spell=7;G.u.filter(x=>x.team==='e'&&!x.dead).forEach(x=>{x.h-=170;if(x.h<=0)kill(x)});blog('Meteor Rain!')}function order(o){if(!G)return;G.ord=o;od.textContent=o[0].toUpperCase()+o.slice(1);document.querySelectorAll('[data-or]').forEach(x=>x.classList.toggle('on',x.dataset.or===o))}function blog(t){let x=document.createElement('div');x.textContent='• '+t;log.prepend(x);while(log.children.length>12)log.lastChild.remove()}
function hud(){mn.textContent=Math.floor(G.mana);wv.textContent=G.w+'/6';gd.textContent=G.gold;ki.textContent=G.kills;ct.textContent=G.u.filter(x=>x.team==='p'&&!x.dead).length;php.style.width=Math.max(0,100*G.php/G.mph)+'%';ehp.style.width=Math.max(0,100*G.ehp/G.meh)+'%'}function db(){ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality='high';ctx.drawImage(A.bg.im,0,0,1600,720);let g=ctx.createLinearGradient(0,0,0,720);g.addColorStop(0,'rgba(3,7,12,.08)');g.addColorStop(.62,'rgba(3,5,8,.04)');g.addColorStop(1,'rgba(0,0,0,.55)');ctx.fillStyle=g;ctx.fillRect(0,0,1600,720);let v=ctx.createRadialGradient(800,350,180,800,350,900);v.addColorStop(0,'rgba(0,0,0,0)');v.addColorStop(1,'rgba(0,0,0,.46)');ctx.fillStyle=v;ctx.fillRect(0,0,1600,720);ctx.fillStyle='rgba(175,190,205,.055)';for(let i=0;i<6;i++){let x=((performance.now()*.012+i*311)%1900)-150;ctx.beginPath();ctx.ellipse(x,575+i%2*24,190,22,0,0,7);ctx.fill()}}
function du(u,dt){let im=u.team==='p'?A.f.im:A.e.im,fr=FR[u.dead?'dead':u.state]||FR.idle;u.an+=dt;let ix=fr[Math.floor(u.an/(u.state==='attack'?.16:u.state==='walk'?.22:.42))%fr.length],fw=im.width/4,fh=im.height/8,sx=(ix%4)*fw,sy=u.row*(fh*2)+Math.floor(ix/4)*fh,base=(u.k==='cavalry'||u.k==='e3')?112:u.hero?120:92,dw=base*u.sc,dh=base*u.sc,ground=u.y;ctx.save();if(u.team==='e'){ctx.translate(u.x,0);ctx.scale(-1,1);ctx.translate(-u.x,0)}ctx.globalAlpha=u.dead?Math.max(0,u.death/1.1):1;ctx.fillStyle='rgba(0,0,0,.48)';ctx.beginPath();ctx.ellipse(u.x,ground+3,dw*.26,6,0,0,7);ctx.fill();ctx.shadowBlur=9;ctx.shadowColor=u.team==='p'?'rgba(65,145,255,.28)':'rgba(255,62,48,.22)';ctx.drawImage(im,sx,sy,fw,fh,u.x-dw/2,ground-dh+10,dw,dh);ctx.shadowBlur=0;ctx.restore();if(!u.dead&&u.h<u.mh){let w=Math.max(34,dw*.52),x=u.x-w/2,y=ground-dh+2;ctx.fillStyle='rgba(0,0,0,.75)';ctx.fillRect(x-1,y-1,w+2,5);ctx.fillStyle=u.team==='p'?'#4da8ff':'#ef4b4b';ctx.fillRect(x,y,w*Math.max(0,u.h/u.mh),3)}}
function upd(dt){if(!G?.started||G.pause||G.over)return;dt*=G.sp;G.mana=Math.min(500,G.mana+17*dt);G.spell=Math.max(0,G.spell-dt);Object.keys(G.cool).forEach(k=>G.cool[k]=Math.max(0,G.cool[k]-dt));G.next-=dt;if(G.next<=0&&G.q.length){en();G.next=G.gap}let ps=G.u.filter(x=>x.team==='p'&&!x.dead),es=G.u.filter(x=>x.team==='e'&&!x.dead);for(const u of G.u){if(u.dead){u.death-=dt;continue}u.c=Math.max(0,u.c-dt);let pool=u.team==='p'?es:ps,near=null,dist=1e9;for(const o of pool){let d=Math.abs(o.x-u.x);if(d<dist){dist=d;near=o}}let dir=u.team==='p'?1:-1,castle=u.team==='p'?1510:90,stop=G.ord==='hold'?690:G.ord==='retreat'?300:2e3;if(near&&dist<=u.r+(u.range?35:0)){u.state='attack';if(u.c<=0){u.an=0;u.c=u.cd;if(u.range)G.sh.push({t:near,x:u.x+dir*20,y:u.y-72,v:dir*580,a:u.a,team:u.team});else{near.h-=u.a;if(near.h<=0)kill(near)}}}else{if(u.team==='p'&&G.ord==='hold'&&u.x>=stop)u.state='idle';else if(u.team==='p'&&G.ord==='retreat'&&u.x>stop){u.state='walk';u.x-=u.s*dt}else{u.state='walk';u.x+=dir*u.s*dt}if((u.team==='p'&&u.x>=castle)||(u.team==='e'&&u.x<=castle)){u.state='attack';if(u.c<=0){u.c=u.cd;if(u.team==='p')G.ehp-=u.a;else G.php-=u.a}}}}
for(let i=G.sh.length-1;i>=0;i--){